{% extends "layout.twig" %}

{% block content %}
<h1 style="text-align: center;">USERS</h1>
<h3 style="text-align: center;">P&aacute;ginas de Usu&aacute;rios</h3>
<div style="text-align: center; margin-bottom: 20px;"">
    <a href="/">Homepage</a>
</div>
<table>
    <thead>
        <tr>
            {% for col in cols %}
            <th>{{ col }}</th>
            {% endfor %}
        </tr>
    </thead>
    <tbody>
        {% for user in users %}
        <tr>
            <td>{{ user.nome }}</td>
            <td>{{ user.login }}</td>
            <td>
                {% for telefone in user.telefones %}
                {{ telefone.numero }}<br>
                {% else %}
                ---
                {% endfor %}
            </td>
        </tr>
        {% endfor %}
    </tbody>
</table>
TOTAL de usuários: {{total}}<br>
<h6 style="text-align: center;">by <a href="http://twitter.com/mlalbuquerque">@mlalbuquerque</a></h6>
{% endblock %}
